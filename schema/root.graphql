type User {
  email: String!
}

type Forbidden {
  redirect: String!
}

union Me = User | Forbidden

enum Location {
  TOP
  ADMIN
}

type Query {
  me(pathname: String, location: Location): Me!
}

# type Mutation {
# }